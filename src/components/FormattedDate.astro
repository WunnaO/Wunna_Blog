---
const { frontmatter } = Astro.props;
---

<time datetime={frontmatter}>
  {
    new Date(frontmatter).toLocaleDateString("en-us", {
      year: "numeric",
      month: "short",
      day: "numeric",
    })
  }
</time>
